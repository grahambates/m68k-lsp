# PUBLIC - Flag symbol as external

## Syntax
```assembly
PUBLIC <symbol>[,<symbol>...]
```

## Description
Flag `<symbol>` as an external symbol, which means that `<symbol>` is visible to all modules in the linking process.
It may be either defined or undefined.
