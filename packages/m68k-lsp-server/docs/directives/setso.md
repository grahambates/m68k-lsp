# SETSO - Set structure offset

## Syntax
```assembly
SETSO <expression>
```

## Description
Sets the structure offset counter (`__SO`) to `<expression>`.
See `so` directive. `<expression>` must evaluate as a constant!
